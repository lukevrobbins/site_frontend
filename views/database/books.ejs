<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <%- include('../partials/header', { title: title }) %>
  
  <main class="container">
    <section class="about-section">
      <h2>Database Books</h2>
      
      <% if (books && books.length > 0) { %>
        <div class="table-container" style="overflow-x: auto; margin: 20px 0;">
          <table style="width: 100%; border-collapse: collapse; margin: 25px 0; font-size: 0.9em; min-width: 400px; border-radius: 5px 5px 0 0; overflow: hidden; box-shadow: 0 0 20px rgba(0, 0, 0, 0.15);">
            <thead>
              <tr style="background-color: #bbeeca; color: #333; text-align: left; font-weight: bold;">
                <% if (books[0]) { %>
                  <% Object.keys(books[0]).forEach(key => { %>
                    <th style="padding: 12px 15px;"><%= key %></th>
                  <% }); %>
                <% } %>
              </tr>
            </thead>
            <tbody>
              <% books.forEach((book, index) => { %>
                <tr style="border-bottom: 1px solid #dddddd; background-color: <%= index % 2 === 0 ? '#f3f3f3' : 'white' %>;">
                  <% Object.values(book).forEach(value => { %>
                    <td style="padding: 12px 15px;"><%= value %></td>
                  <% }); %>
                </tr>
              <% }); %>
            </tbody>
          </table>
        </div>
      <% } else { %>
        <p>No books found in the database.</p>
      <% } %>
      
      <div style="margin-top: 20px;">
        <a href="/database/test" style="display: inline-block; padding: 10px 15px; background-color: #bbeeca; color: #333; text-decoration: none; border-radius: 4px; transition: background-color 0.3s;">Test Database Connection</a>
        <a href="/" style="display: inline-block; padding: 10px 15px; background-color: #f3f3f3; color: #333; text-decoration: none; border-radius: 4px; margin-left: 10px; transition: background-color 0.3s;">Back to Home</a>
      </div>
    </section>
  </main>
</body>
</html>